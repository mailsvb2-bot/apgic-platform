version: 1
environment: PRODUCTION
production_approved: false
jurisdiction_matrix_version: CONFIG_REQUIRED
retention_policy_version: CONFIG_REQUIRED
slo_policy_version: CONFIG_REQUIRED
provider_matrix_version: CONFIG_REQUIRED
market_cell_thresholds_version: CONFIG_REQUIRED
notes:
  - "R1 example only. Production promotion must fail until all R0+R1 policy paths and MarketCell thresholds are explicitly approved."
