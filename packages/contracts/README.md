# @apgic/contracts

Shared client boundary for generated/versioned schemas, stable enums, reason codes and validation metadata.

This package must not contain APGIC server business logic. Web, iOS and Android consume the same server-owned truth through versioned contracts.
