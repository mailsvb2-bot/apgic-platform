# Contract Registry

This directory indexes versioned domain/API/event/schema contracts. Public transport schemas live under `/contracts`; this directory owns their canon linkage, lifecycle and compatibility metadata.
