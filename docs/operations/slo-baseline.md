# SLO baseline placeholder

APGIC-CONFIG-005 and APGIC-NFR-001 are not VERIFIED by this file.

Before production promotion, the repository must contain versioned SLI/SLO definitions for each active critical endpoint, job and provider path, plus alert expectations and dependency attribution. LaunchConfig must reference the approved SLO policy version.

The bootstrap health endpoints are probes only; they are not production observability evidence.
